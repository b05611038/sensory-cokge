numpy
igraph
matplotlib
scikit-learn
torch
pandas
transformers>=4.42.0
transformers[tensorboard]
datasets
sentencepiece
protobuf
opentsne
adjustText
tensorboardX
pyyaml

# Optional dependencies (not required for core functionality)
# laser_encoders - only needed for Laser_embeddings function (used in paper results)
